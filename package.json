{
  "name": "bsn-silent-updates",
  "version": "1.0.6",
  "description": "BSN with silent update mechanism",
  "scripts": {
    "deploy": "node deploy-update.js",
    "update": "node update-version.js",
    "test": "echo 'Open update-test.html in your browser to test updates'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0"
  }
}
